/** Compact red pill for either-direction block status in recipient pickers. */
export function BlockedPeerBadge() {
  return (
    <span className="shrink-0 rounded-full bg-danger-500 px-2 py-0.5 text-[10px] font-semibold leading-none text-white">
      Blocked
    </span>
  );
}
